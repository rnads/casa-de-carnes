import React from 'react';
import { ScrollView, View } from 'react-native';

export function Producst(){
    return (
        <ScrollView>
            <View>

            
            </View>
            <Text>Prod</Text>
        </ScrollView>
    )
}
